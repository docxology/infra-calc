# AGENTS.md — `infra-calc/js/precalculated`

> Local-only path under `projects/ongoing/DataTools/` — matched by the
> root `.gitignore` rule `projects/*`; never commit. Repo-wide policy:
> see `/Volumes/external_drive/Git/template/projects/ongoing/AGENTS.md`.

## What this is

Generated by `run.py` each run (written at `run.py` ~line 1471): `results.js`
is a JS file embedding the computed economics so the browser UI can render
charts without recalculation. Do not hand-edit — rerun `python3 run.py` after
changing parameters.

## Layout (verified by direct listing, 2026-08-29)

```
results.js
```

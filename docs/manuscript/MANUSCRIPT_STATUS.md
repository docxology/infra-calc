# Manuscript Status

- **Repo type:** Tool / interactive web calculator (LLM inference economics).
- **Why no publication-target manuscript applies today:** evidence checked —
  the repo is a UI + calculation driver (`index.html`, `js/`, `run.py`) with
  no analysis outputs, figures of record, or research findings; the root
  `README.md` frames it as a decision-support tool.
- **What would trigger creating a `manuscript/`:** an original study using the
  calculator's model (e.g. a published cost/utilization analysis with
  reproducible outputs and figures).
